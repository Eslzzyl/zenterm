"""
Terminal Render Test Suite — cross-platform terminal emulator rendering tests.

Tests SGR attributes, colors (16/256/truecolor), cursor behaviour, Unicode
rendering, emoji, ligatures, OSC sequences, and graphics protocols (Sixel,
Kitty) across all modern terminal emulators.

Usage:
    python -m zenterm_test --help
"""

__version__ = "0.1.0"
