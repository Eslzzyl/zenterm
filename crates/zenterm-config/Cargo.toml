[package]
name = "zenterm-config"
version.workspace = true
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
log = "0.4"
thiserror = "2"

zenterm-core = { path = "../zenterm-core" }
