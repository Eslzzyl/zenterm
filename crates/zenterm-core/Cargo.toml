[package]
name = "zenterm-core"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61", features = [
    "Win32_UI_WindowsAndMessaging",
] }
