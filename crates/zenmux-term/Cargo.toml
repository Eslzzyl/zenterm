[package]
name = "zenmux-term"
version = "0.1.0"
edition = "2024"

[dependencies]
zenmux-core = { path = "../zenmux-core" }
alacritty_terminal = "0.26"
vte = { version = "0.15", features = ["ansi"] }
log = "0.4"
